# trade_flow Tech Spec

> **작성일**: 2026-07-21
> **작성자**: Codex
> **버전**: v1.1
> **상태**: Approved — Phase 1

---

## 0. 정의 (Definitions)

### 0.1 도메인 용어

| 용어 | 정의 |
|------|------|
| 거래일 D | 주문을 제출하는 미국 정규장 거래일이다. |
| 신호 기준일 | D 직전의 완료된 미국 정규장 거래일이다. |
| 신호 데이터 | 신호 기준일까지 확정된 일봉 데이터만 포함한 전략 입력이다. |
| 유니버스 등급 A | 공식 데이터로 각 거래일의 실제 구성 종목을 재현한 point-in-time 유니버스이다. |
| 유니버스 등급 B | 공개 편입·제외 이력으로 구성 종목을 근사 복원하고 출처와 누락을 기록한 유니버스이다. |
| 유니버스 등급 C | 현재 구성 종목을 전체 과거 기간에 적용한 생존 편향 유니버스이다. |
| 원시 목표 비중 | 가격 기반 전략만 계산한 종목별 목표 비중이다. |
| 최종 목표 비중 | 전략 결과에 리스크 정책을 적용한 종목별 목표 비중과 현금 비중이다. |
| 실행 ID | 한 번의 프로세스 실행을 추적하는 UUID이다. 멱등성 판단에는 사용하지 않는다. |
| 주문 의도 ID | `account_id/trading_date/strategy_version/symbol/side/rebalance_seq`로 생성한 결정적 식별자이다. |
| 신규 매수 중단 정책 | 위험 레짐 동안 모든 매수 주문을 금지하고 전략 청산과 손절 매도만 허용하는 정책이다. |
| 노출 축소 정책 | 위험 레짐 동안 주식 목표 비중을 정해진 상한까지 비례 축소하는 실험 정책이다. |
| 데이터 신선도 | 신호 데이터가 신호 기준일까지 완결되고 공급자 타임스탬프와 시장 캘린더가 일치하는 상태이다. |
| 포지션 정합성 | 동일 신호·설정·데이터의 목표 종목·방향·수량이 일치하고 실제 차이가 허용 사유로 설명되는 상태이다. |
| 치명 오류 | 중복 주문, 반대 방향 주문, 보유 수량 초과 매도, 리스크 한도 초과 주문, 계좌 불일치 주문이다. |

### 0.2 정책 수치

| 정책 | 값 | 적용 단계 |
|------|-----|-----------|
| 실행 시각 기본값 | 거래일 D 09:31 America/New_York | Phase 2에서 확정 |
| 실행 허용 지연 | 예정 시각 이후 15분 | Phase 2에서 확정 |
| 메인 선정 수 N | 기본값 5, 검증 범위 3~5 | Phase 1 |
| 모멘텀 기간 | 60거래일 | Phase 1 |
| SMA 기간 | 50거래일, 200거래일 | Phase 1 |
| RSI 기간과 신호 | RSI14 < 50이면 1, 아니면 0 | Phase 1 |
| MACD 기간과 신호 | 12·26·9, MACD line > signal line이면 1 | Phase 1 |
| 팩터 가중치 기본값 | 모멘텀 0.40, 추세 0.25, RSI 0.15, MACD 0.20 | Phase 1 |
| 동점 거래대금 기간 | 최근 20거래일 | Phase 1 |
| 손절 기준 | 평균 체결단가 대비 신호 기준일 종가 -10% 이하 | Phase 1 |
| 메인 슬리브 총 목표 비중 | 88% | Phase 1 |
| 일반 종목당 비중 상한 | 25% | Phase 1 |
| 고변동 슬리브 총 비중 상한 | 10% | 전 단계 |
| 고변동 종목당 비중 상한 | 5% | 전 단계 |
| 고변동 선정 상한 | 2종목 | Phase 1 |
| 주문 현금 여유분 | 확정 NAV의 2% | Phase 2에서 확정 |
| 일일 손실 한도 기본값 | 전 거래일 확정 NAV 대비 -3% | Phase 2에서 확정 |
| 위험 레짐 진입 | VIX 종가 > 30 또는 WTI 20거래일 수익률 > 30% | Phase 1 |
| 위험 레짐 해제 | 진입 조건이 3거래일 연속 거짓 | Phase 1 |
| 노출 축소 정책 주식 상한 | 확정 NAV의 50% | Phase 1 실험에만 사용 |
| 지정가 허용 폭 기본값 | 기준 가격 대비 매수 +0.30%, 매도 -0.30% | Phase 2에서 확정 |
| 주문 유효시간 기본값 | 제출 후 10분 | Phase 2에서 확정 |
| 체결 조회 주기 기본값 | 5초 | Phase 2에서 확정 |
| API 조회 재시도 | 최대 3회, 1초·2초·4초 지수 백오프 | Phase 2 |
| 최소 주문 수량 | 정수 1주 | 전 단계 |
| 최소 가격 데이터 | 201거래일 | Phase 1 |
| 가격 데이터 허용 결측 | 최근 20거래일 0건 | 전 단계 |
| 감정점수 수집 후보 상한 | 기술 팩터 상위 20종목 | Phase 2 |
| 감정점수 최소 관찰 기간 | 60거래일 | Phase 2 |
| 감정점수 평가 구간 | 다음 1·5·20거래일 | Phase 2 |
| 백테스트 최소 기간 | 10년 | Phase 1 |
| 레짐 이벤트 스터디 기간 | VIX·WTI 공통 데이터의 최대 가용 기간, 목표 30년 | Phase 1 |
| 백테스트 holdout | 최신 2년 | Phase 1 |
| walk-forward 구간 | 학습 5년, 검증 1년 | Phase 1 |
| 거래비용 민감도 | 편도 5bp, 15bp, 30bp | Phase 1 |
| 모의투자 최소 기간 | 연속 20거래일 | Phase 2 |
| 유니버스 C 보완 검증 | 추가 연속 60거래일 모의투자 | Phase 2 |
| 실계좌 dry-run 기간 | 연속 5거래일 | Phase 3 |
| 실계좌 소액 검증 기간 | 연속 20거래일 | Phase 3 |
| 포지션 허용 차이 | 정수 내림·현금 여유분·수수료·부분 체결·미체결 취소로 설명 가능한 차이만 허용 | Phase 2 |
| 로그 보존 기간 | 365일 | 운영 |
| DB 백업 보존 | 일별 30개 | 운영 |

### 0.3 불변식 (Invariants)

| Invariant | 설명 |
|-----------|------|
| 전략은 순수 함수이다 | 동일한 입력과 설정은 의미상 동일한 점수, 순위, 목표 비중을 반환한다. |
| 미래 데이터를 사용하지 않는다 | 신호 계산에는 신호 기준일 이후의 가격·감정·체결 정보를 사용하지 않는다. |
| 불확실하면 거래하지 않는다 | 잔고, 미체결 주문, 데이터 신선도, 계좌 중 하나라도 확인할 수 없으면 주문하지 않는다. |
| 주문 상태는 브로커가 원장이다 | 로컬 DB와 브로커가 다르면 신규 주문을 중단하고 브로커 상태로 조정한다. |
| 매도를 먼저 처리한다 | 매도 체결과 취소를 확인한 뒤 실제 주문 가능 현금으로 매수 수량을 계산한다. |
| 주문 의도는 멱등하다 | 같은 주문 의도 ID는 재실행과 장애 복구 과정에서 한 번만 제출한다. |
| 실계좌는 명시적으로 활성화한다 | production 환경, 계좌 allowlist, kill switch 해제를 모두 확인한다. |
| 공매도와 레버리지를 사용하지 않는다 | 보유 수량 초과 매도, 신용, 마진, 옵션 주문을 생성하지 않는다. |
| 정수 수량만 주문한다 | 소수점 주식을 사용하지 않고 잔여 금액은 현금으로 유지한다. |
| 레짐 기본 동작은 신규 매수 중단이다 | 노출 축소 정책은 Phase 1 승인 전에는 paper와 production 주문에 적용하지 않는다. |
| 일봉 손절은 장중 손실을 보장하지 않는다 | 손절은 신호 기준일 종가로 판정하고 D의 주문으로 실행한다. |
| 감정점수는 주문에 관여하지 않는다 | 별도 승인 전에는 감정점수를 전략 입력과 최종 목표 비중에 전달하지 않는다. |
| 팩터 정의는 이 문서가 기준이다 | Section 0.2 정의를 구현하며 Notion 문서는 배경 자료로만 사용한다. |
| 시간대 없는 시각을 사용하지 않는다 | 시장 이벤트는 America/New_York, 저장은 UTC, 사용자 표시는 Asia/Seoul을 사용한다. |
| 생존 편향과 거래비용을 분리 평가한다 | 높은 거래비용 가정으로 유니버스 생존 편향을 대체하지 않는다. |
| 배당을 이중 반영하지 않는다 | split-adjusted 가격과 현금 배당을 사용하며 total-return 수정주가와 현금 배당을 함께 사용하지 않는다. |

---

## 1. 개요 (Overview)

### 1.1 배경 및 목적

한국투자증권 Open API를 이용하여 미국주식 일봉 스윙 전략을 백테스트, 모의투자, 실계좌에서 동일한 전략 계약으로 실행한다. 프로그램은 거래일마다 한 번 실행하고 주문·체결·포트폴리오 상태를 재현 가능한 감사 기록으로 남긴다.

### 1.2 목표

- 백테스트와 라이브 실행기가 동일한 전략 함수를 호출한다.
- 주문 전에 데이터, 계좌, 리스크, 중복 여부를 검증한다.
- 장애 후 재실행해도 중복 주문이 발생하지 않는다.
- 전략 성과와 운영 안정성을 별도 관문으로 검증한다.
- 미국시장 검증이 끝난 뒤 브로커 경계를 유지한 채 한국시장으로 확장한다.

### 1.3 비목표

- 장중 실시간 손절과 웹소켓 시세 처리는 포함하지 않는다.
- 공매도, 레버리지, 옵션, 소수점 매매는 포함하지 않는다.
- 자동 환전과 통합증거금 자동 사용은 포함하지 않는다.
- 뉴스 또는 LLM의 해석으로 주문을 생성하지 않는다.
- 웹 대시보드와 다중 사용자 권한 시스템은 포함하지 않는다.
- 외부 ML 모델 개발은 포함하지 않는다.
- 수익 또는 원금 보장을 목표로 하지 않는다.

---

## 2. 작업 범위 (Scope)

| 구분 | 항목 | 설명 |
|------|------|------|
| In Scope | 일봉 수집 | 미국주식, VIX, WTI 데이터를 수집하고 검증한다. |
| In Scope | 전략 계산 | 가격 기반 팩터를 계산해 원시 목표 비중을 만든다. |
| In Scope | 백테스트 | 동일 전략 계약으로 비용·슬리피지·다음 날 체결을 시뮬레이션한다. |
| In Scope | 브로커 연동 | KIS 인증, 계좌, 잔고, 주문, 체결 조회를 캡슐화한다. |
| In Scope | 주문 실행 | 매도 우선, 지정가, 미체결 취소, 멱등성, reconciliation을 처리한다. |
| In Scope | 위험 통제 | 비중, 현금, 손실, 데이터 상태, kill switch를 검사한다. |
| In Scope | 운영 기록 | 실행, 주문, 체결, 스냅샷, 경보를 SQLite와 파일 로그에 남긴다. |
| In Scope | 알림 | 텔레그램으로 실행 결과와 경보를 전송한다. |
| In Scope | 감정 관찰 | 후보 종목의 감정점수를 주문과 분리해 저장하고 평가한다. |
| Out of Scope | 한국주식 | 미국시장 승인 이후 별도 spec으로 정의한다. |
| Out of Scope | 장중 대응 | 별도 실시간 시스템 없이 일봉 실행만 지원한다. |
| Out of Scope | 자동 환전 | 달러 예수금 부족분은 주문 축소와 경보로 처리한다. |

---

## 3. 기능 명세 (Functional Specification)

### 3.1 실행 오케스트레이션

**전제 조건**

- 실행 환경이 `backtest`, `paper`, `dry-run`, `production` 중 하나로 명시되어 있다.
- 대상 계좌가 해당 환경의 allowlist와 일치한다.
- 미국시장 캘린더가 거래일 D와 신호 기준일을 반환한다.

**정상 흐름**

| 단계 | 동작 | 결과 |
|------|------|------|
| 1 | 프로세스 락을 획득하고 실행 ID를 생성한다. | 중복 프로세스가 차단된다. |
| 2 | kill switch, 환경, 계좌 ID를 검증한다. | 주문 가능 여부가 확정된다. |
| 3 | 브로커 잔고와 미체결 주문을 조회해 로컬 상태와 조정한다. | 시작 상태가 확정된다. |
| 4 | 시장 캘린더와 실행 허용 지연을 검사한다. | 거래일과 실행 가능 시간이 확정된다. |
| 5 | 신호 데이터와 레짐 데이터를 수집하고 신선도를 검증한다. | 전략 입력 스냅샷이 생성된다. |
| 6 | 전략이 원시 목표 비중을 계산한다. | 가격 기반 후보와 비중이 생성된다. |
| 7 | 리스크 정책이 손절과 노출 한도를 반영한다. | 최종 목표 비중이 확정된다. |
| 8 | 현재 상태와 최종 목표 비중의 차이로 주문 계획을 만든다. | 매도·매수 주문 의도가 생성된다. |
| 9 | 매도를 먼저 실행하고 브로커 상태를 다시 조회한다. | 실제 주문 가능 현금이 확정된다. |
| 10 | 현금 범위에서 매수 수량을 다시 계산하고 실행한다. | 목표 포트폴리오에 근접한다. |
| 11 | 미체결 주문을 취소하고 최종 체결·잔고를 조정한다. | 종료 스냅샷이 확정된다. |
| 12 | DB와 파일 로그를 기록하고 알림을 전송한다. | 실행 결과가 감사 가능하게 보존된다. |

**예외 흐름**

| 조건 | 처리 방식 |
|------|----------|
| 휴장일이다 | 주문 없이 성공 종료하고 실행 사유를 기록한다. |
| 실행 허용 지연을 초과했다 | 주문 없이 실패 종료하고 경보를 전송한다. |
| 락이 이미 존재한다 | 기존 프로세스 생존 여부를 확인하고, 생존 중이면 중복 실행으로 종료한다. |
| 이전 프로세스가 남긴 stale lock이다 | 기존 실행 상태를 조정한 뒤 새 락을 획득한다. |
| 데이터가 누락되거나 오래되었다 | 신규 주문과 축소 매도를 모두 중단하고 경보를 전송한다. |
| 잔고 또는 미체결 주문 조회가 실패한다 | 주문 없이 실패 종료한다. |
| 텔레그램 전송이 실패한다 | 실행 결과는 유지하고 로그에 경보 실패를 기록해 scheduler 감시 대상으로 노출한다. |
| 처리되지 않은 예외가 발생한다 | 비정상 종료 코드를 반환하고 가능한 범위에서 실행 실패를 기록한다. |

### 3.2 데이터 수집과 검증

**입력**

- 설정 파일의 유니버스와 공급자 심볼 매핑
- 미국시장 캘린더
- 가격·레짐·감정 데이터 공급자

**처리 규칙**

- OHLCV는 신호 기준일까지의 행만 전략 입력에 포함한다.
- 백테스트 신호에는 split-adjusted OHLC를 사용하고 배당은 포트폴리오 현금흐름에 한 번만 반영한다.
- 라이브 데이터에는 원본 가격과 공급자 수정 여부를 함께 저장한다.
- 원본 심볼, 데이터 공급자 심볼, KIS 주문 심볼은 설정 파일에서 매핑한다.
- 심볼 변경, 분할, 상장폐지는 발견 시 설정 파일의 유효기간 매핑으로 추가한다.
- 가격 역전, 음수 거래량, 중복 키, 캘린더 불일치를 유효성 오류로 처리한다.
- 레짐 입력이 유효하지 않으면 안전 상태로 전환하고 신규 매수를 금지한다.

**출력**

- `MarketDataSnapshot(as_of, prices, regime_inputs, quality_report)`
- 품질 보고서는 누락, 중복, 최신 거래일, 공급자 타임스탬프를 포함한다.

### 3.3 가격 기반 전략

**팩터 정의**

| 팩터 | 값의 정의 | 정규화 |
|------|-------------|--------|
| 모멘텀 | 신호 기준일 종가 ÷ Section 0.2의 모멘텀 기간 전 종가 - 1 | 적격 종목 내 percentile rank 0~1 |
| 추세 | Section 0.2의 단기 SMA가 장기 SMA보다 크면 1, 아니면 0 | 0 또는 1 |
| RSI | Section 0.2의 RSI 신호를 만족하면 1, 아니면 0 | 0 또는 1 |
| MACD | Section 0.2의 MACD 신호를 만족하면 1, 아니면 0 | 0 또는 1 |

**적격 조건**

- 신호 기준일 종가가 Section 0.2의 장기 SMA보다 크다.
- 최소 가격 데이터와 데이터 신선도 조건을 만족한다.
- 거래정지, 상장폐지 진행, 주문 불가 심볼이 아니다.
- 가격이 0보다 크고 Section 0.2의 최소 주문 수량을 주문할 수 있다.

**선정과 비중**

- 총점은 Section 0.2의 팩터 가중치와 팩터 값의 가중합이다.
- 동점이면 모멘텀, Section 0.2 기간의 평균 거래대금, 심볼 오름차순으로 결정한다.
- 메인 슬리브는 적격 종목 중 상위 N개에 Section 0.2의 총 목표 비중을 균등 배분한다.
- 고변동 슬리브는 별도 설정 목록에서 상위 종목을 선정하고 슬리브 한도를 적용한다.
- 적격 종목 수가 N보다 적으면 남는 비중은 현금으로 유지한다.
- 전략 출력은 음수 비중을 포함하지 않으며 전체 합이 1을 넘지 않는다.

**출력 계약**

```python
@dataclass(frozen=True)
class StrategyResult:
    as_of: date
    target_weights: Mapping[str, Decimal]
    cash_weight: Decimal
    scores: Mapping[str, FactorScore]
    exclusions: Mapping[str, str]

def signal(snapshot: MarketDataSnapshot, config: StrategyConfig) -> StrategyResult: ...
```

### 3.4 위험 레짐과 리스크 정책

- 위험 레짐의 기본 동작은 신규 매수 중단 정책이다.
- 위험 레짐에서도 전략 청산, 손절 매도, 비중 상한 초과분 매도는 허용한다.
- 노출 축소 정책은 Phase 1 백테스트 결과가 승인되기 전까지 주문에 적용하지 않는다.
- 손절 대상은 전략 순위와 무관하게 최종 목표 비중을 0으로 만든다.
- 일일 손실 한도가 발동하면 매수를 금지하고 축소·손절 매도만 허용한다.
- 종목별·슬리브별 상한 초과분은 현금으로 유지한다.
- 달러 주문 가능 금액이 부족하면 매수 목표를 축소하며 매도 예상대금은 실제 체결 전까지 사용하지 않는다.
- 현재가, 잔고, 평균 체결단가 중 필요한 값이 없으면 해당 종목의 주문을 생성하지 않는다.

**출력 계약**

```python
def apply_risk_policy(
    strategy_result: StrategyResult,
    account: AccountSnapshot,
    regime: RegimeState,
    config: RiskConfig,
) -> RiskAdjustedTarget: ...
```

### 3.5 주문 계획과 집행

**주문 수량**

- 목표 금액은 확정 NAV와 목표 비중의 곱이다.
- 목표 수량은 목표 금액을 기준 가격으로 나눈 값의 내림 정수이다.
- 현재 수량과 목표 수량의 차이가 주문 수량이다.
- Section 0.2의 주문 현금 여유분과 예상 수수료를 제외한 주문 가능 현금 안에서만 매수한다.
- 최소 주문 수량을 만들 수 없는 차이는 주문하지 않고 drift로 기록한다.

**지정가와 체결**

- 기준 가격은 주문 직전 조회한 유효한 최우선 호가이며 호가가 없으면 주문을 생성하지 않는다.
- 지정가는 Section 0.2의 허용 폭을 적용하고 거래소 호가 단위로 보수적으로 반올림한다.
- 주문 의도는 주문 API 호출 전에 DB에 먼저 저장한다.
- 주문 제출 전에 주문 의도 ID로 로컬 DB와 브로커 주문을 함께 조회한다.
- 주문 유효시간 동안 체결 상태를 조회하고 남은 수량은 취소한다.
- 부분 체결 수량만 잔고와 현금에 반영한다.
- 취소 확인이 실패하면 후속 매수를 금지하고 수동 확인 경보를 전송한다.
- 다음 실행은 남은 목표 차이를 새 시장 상태로 다시 계산하며 이전 주문을 이월하지 않는다.

**재실행과 복구**

- 시작 시 전일 이후의 미체결·체결 주문을 브로커에서 조회한다.
- 주문 의도 ID가 이미 제출된 경우 새 주문을 만들지 않고 기존 주문 상태를 추적한다.
- 브로커가 사용자 정의 주문 ID를 지원하지 않으면 주문 시각, 심볼, 방향, 수량, 가격을 조합한 보조 대조키를 저장한다.
- 주문 응답을 받지 못한 네트워크 타임아웃은 즉시 재주문하지 않고 브로커 주문 조회로 존재 여부를 확인한다.

### 3.6 감정점수 관찰

- Section 0.2의 감정점수 수집 후보 상한까지만 수집 대상으로 삼는다.
- 기사별 티커 관련도와 감정점수의 가중 평균, 기사 수, 결측 사유를 저장한다.
- 기사가 없는 날은 0점이 아니라 `missing`으로 저장한다.
- 감정점수는 목표 비중과 주문 계획에 전달하지 않는다.
- 관찰 기간 종료 후 사전 정의된 평가 보고서를 생성하고 별도 승인 문서에서 반영 여부를 결정한다.
- 평가 보고서는 커버리지, Section 0.2의 감정점수 평가 수익 구간과의 rank IC, 포트폴리오 교체율, 비용 차감 성과를 포함한다.

### 3.7 알림과 운영 상태

**일일 결과 알림**

- 실행 ID와 환경
- 신호 기준일과 데이터 품질
- 레짐 상태와 입력값
- 목표·실제 포지션 차이
- 제출·체결·취소·실패 주문
- 현금, NAV, 일일 손익
- 감정점수 관찰 요약
- 수동 확인 필요 여부

**운영 감시**

- scheduler는 프로세스 종료 코드와 최신 성공 실행 시각을 감시한다.
- 거래일의 실행 허용 시간이 끝날 때 성공 또는 명시적 휴장 기록이 없으면 외부 dead-man 경보를 발생시킨다.
- kill switch가 활성화된 실행도 주문 없이 상태와 경보를 기록한다.

---

## 4. 비기능 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 | 검증 방법 |
|------|----------|-----------|
| 결정성 | 동일 입력과 설정은 의미상 동일한 점수, 순위, 목표 비중을 만든다. | 단위 테스트 |
| 실행 시간 | Section 0.2의 실행 허용 지연 안에 데이터 검증과 주문 계획을 완료한다. | 실행 로그 |
| 신뢰성 | Section 0.2의 모의투자 최소 기간 동안 치명 오류가 없다. | 주문 감사 로그 |
| 복구성 | 주문 응답 유실, 프로세스 강제 종료, stale lock에서 중복 주문 없이 복구한다. | 장애 주입 테스트 |
| 보안 | 시크릿과 전체 계좌번호를 코드, DB, 알림, 로그에 기록하지 않는다. | 정적 검사·로그 점검 |
| 파일 권한 | `.env`, 토큰 캐시, SQLite, 백업은 실행 사용자만 읽고 쓸 수 있다. | 권한 검사 |
| 감사성 | 실행 입력 해시, 설정 버전, 주문 의도, 브로커 응답 식별자를 추적할 수 있다. | 표본 실행 재구성 |
| 백업 | SQLite를 매일 무결성 검사 후 일관된 스냅샷으로 백업한다. | 복구 훈련 |
| 시간대 | naive datetime을 저장하거나 비교하지 않는다. | 타입·단위 테스트 |
| 알림 | 알림 실패가 거래 성공으로 은폐되지 않고 scheduler 상태에 노출된다. | 통합 테스트 |

---

## 5. 데이터 모델 (Data Model)

금액과 수량은 이진 부동소수점이 아닌 고정소수점 문자열 또는 정수 최소단위로 저장한다. 초기 버전은 아래 테이블만 사용하고 심볼 이력과 알림 상태는 설정 파일과 `runs` 필드로 관리한다.

| 테이블 | 주요 필드 | 제약조건 |
|--------|-----------|----------|
| `prices` | symbol, session_date, OHLCV, split-adjusted OHLC, cash_dividend, source, fetched_at | `(symbol, session_date, source)` unique |
| `sentiment` | symbol, session_date, score, relevance, article_count, source, missing_reason | `(symbol, session_date, source)` unique |
| `orders` | intent_id, run_id, broker_order_id, symbol, side, requested_qty, limit_price, status, error_code | intent_id unique |
| `fills` | broker_fill_id, broker_order_id, quantity, price, fee, filled_at | broker_fill_id unique |
| `snapshots` | run_id, captured_at, NAV, cash, positions, source | `(run_id, captured_at, source)` unique |
| `runs` | run_id, environment, account_hash, trading_date, signal_date, data_hash, config_hash, universe_hash, status, notification_status, timestamps | run_id unique |

**상태값**

- 실행 상태: `started`, `skipped`, `blocked`, `completed`, `failed`, `needs_review`
- 주문 상태: `planned`, `submitted`, `partially_filled`, `filled`, `cancel_pending`, `cancelled`, `unknown`
- 주문과 체결은 삭제하지 않고 상태와 조정 기록을 추가한다.
- 백업과 로그는 Section 0.2의 보존 정책을 따른다.

---

## 6. 외부 인터페이스 (External Interfaces)

### 6.1 브로커 계약

```python
class Broker(Protocol):
    def account_snapshot(self) -> AccountSnapshot: ...
    def quote(self, symbol: str) -> Quote: ...
    def open_orders(self, since: datetime) -> Sequence[BrokerOrder]: ...
    def submit(self, request: OrderRequest) -> BrokerOrder: ...
    def cancel(self, broker_order_id: str) -> BrokerOrder: ...
    def order(self, broker_order_id: str) -> BrokerOrder: ...
    def fills(self, since: datetime) -> Sequence[Fill]: ...
```

- paper와 production 구현은 동일 계약을 사용하고 base URL, 앱키, 계좌 allowlist를 분리한다.
- 인증 헤더와 원문 응답의 민감 필드는 저장 전에 마스킹한다.
- HTTP 성공 여부와 업무 오류 코드를 분리해 처리한다.
- 조회 API만 자동 재시도하며 주문 API 타임아웃은 Section 3.5의 대조 절차를 따른다.

### 6.2 시장 데이터 계약

```python
class MarketDataProvider(Protocol):
    def daily_bars(self, symbol: str, start: date, end: date) -> Sequence[DailyBar]: ...
    def regime_inputs(self, start: date, end: date) -> Sequence[RegimeInput]: ...
```

- 공급자별 심볼, 수정주가, 캘린더 의미를 adapter가 canonical 모델로 변환한다.
- 백테스트 데이터와 라이브 데이터의 차이를 일별 검증 리포트로 기록한다.

### 6.3 감정 데이터 계약

```python
class SentimentProvider(Protocol):
    def daily_sentiment(self, symbol: str, session_date: date) -> SentimentObservation: ...
```

- rate limit을 넘지 않도록 일일 요청 예산을 설정한다.
- 공급자 오류, 요청 한도, 기사 없음은 서로 다른 결측 사유로 기록한다.

### 6.4 알림 계약

```python
class Notifier(Protocol):
    def send(self, message: Notification) -> DeliveryResult: ...
```

- 알림에는 시크릿, 전체 계좌번호, 전체 브로커 응답을 포함하지 않는다.
- 알림 실패는 DB와 프로세스 종료 상태로 외부 감시에 전달한다.

---

## 7. 검증 및 출시 기준 (Validation & Rollout)

### 7.1 Phase 1 — 백테스트와 전략 승인

- Section 0.2의 백테스트 최소 기간을 포함하고 holdout은 파라미터 선택에 사용하지 않는다.
- 나머지 기간은 Section 0.2의 walk-forward 구간으로 평가한다.
- 유니버스 등급 B를 기본 결과로 사용하고 등급 C를 민감도 결과로 함께 제시한다.
- 등급 B를 구성할 수 없으면 등급 C의 한계를 표시하고 Phase 2까지만 진행한다.
- SPY buy-and-hold와 100% 현금을 비교 기준으로 사용한다.
- 거래비용은 Section 0.2의 거래비용 민감도 결과를 모두 제시한다.
- 신규 매수 중단 정책과 노출 축소 정책을 동일 데이터와 비용 조건으로 평가한다.
- 위험 레짐은 Section 0.2의 이벤트 스터디 기간과 주요 충격 구간을 별도로 보고한다.
- 전체 기간과 주요 충격 구간의 CAGR, MDD, Sharpe, Calmar, 회전율, 승률, 회복기간을 보고한다.

**승인 기준**

- 미래 참조, 중복 봉, 잘못된 수정주가 사용이 0건이다.
- holdout에서 비용 차감 전략 MDD가 SPY보다 작다.
- Section 0.2의 가장 보수적인 비용 시나리오에서 전략의 장기 기대수익이 현금보다 높다.
- 인접 파라미터 조합에서도 성과 방향이 유지되어 단일 최적점 의존이 없다.
- 전략 결과가 고정 fixture와 일치하고 동일 입력에서 결정적이다.
- 승인된 파라미터와 데이터 해시는 변경 불가능한 실행 manifest로 보존한다.

### 7.2 Phase 2 — dry-run과 모의투자 승인

- KIS 모의투자 지원 범위를 확인한 후 주문 계약을 확정한다.
- Section 0.2의 모의투자 최소 기간 동안 연속 모의투자를 수행한다.
- 치명 오류가 0건이다.
- 제출한 모든 주문 의도가 브로커 주문 또는 명시적 미제출 사유와 연결된다.
- 강제 종료와 네트워크 타임아웃 장애 주입 후 중복 주문이 0건이다.
- 동일한 `signal_date`, `data_hash`, `config_hash`, `universe_hash`를 사용한 백테스터와 모의투자의 목표 종목·방향·수량이 일치한다.
- 최종 포지션 차이는 Section 0.2의 포지션 허용 차이로 모두 설명되며 설명 불가 차이가 없다.
- kill switch, stale data, 잔고 조회 실패, 일일 손실 한도, 알림 실패를 각각 한 번 이상 검증한다.
- 거래일 실행 누락이 모두 dead-man 경보로 감지된다.
- 유니버스 등급 B를 확보하지 못하면 Section 0.2의 보완 검증과 사용자 명시적 승인을 추가한다.

### 7.3 Phase 3 — 실계좌 단계적 출시

- Section 0.2의 실계좌 dry-run 기간에는 production 조회와 dry-run만 수행한다.
- 다음 Section 0.2의 실계좌 소액 검증 기간은 별도 승인된 자본 상한으로 운용한다.
- 매 거래일 사용자가 일일 리포트와 조정 결과를 확인한다.
- 치명 오류 또는 설명할 수 없는 잔고 차이가 한 번이라도 발생하면 신규 매수를 중단하고 Phase 2로 되돌린다.
- 자본 상한 확대는 별도 승인 기록과 설정 변경으로만 수행한다.

### 7.4 필수 테스트

- 전략 결정성, 미래 참조, 결측·동점 처리 테스트
- 목표 비중 합계와 종목·슬리브 상한 테스트
- 정수 수량, 현금 부족, 수수료 여유분 테스트
- 손절, 손실 한도, stale data, kill switch 테스트
- 주문 멱등성과 주문 응답 유실 테스트
- 부분 체결과 취소 실패 테스트
- paper와 production 설정 격리 테스트

---

## 8. 열린 질문 및 결정 사항 (Open Questions & Decisions)

| # | 질문 | 결정 내용 | 결정자 | 결정일 |
|---|------|-----------|--------|--------|
| 1 | KIS 모의투자가 필요한 미국주식 주문·정정·취소·체결 조회를 모두 지원하는가? | 미결 | 구현 담당 | - |
| 2 | KIS의 주문 idempotency용 사용자 정의 키 또는 원주문 조회 필드는 무엇인가? | 미결 | 구현 담당 | - |
| 3 | 지정가 허용 폭과 호가 단위는 거래소·종목별로 어떻게 매핑하는가? | 미결 | Phase 2 승인 | - |
| 4 | 유니버스 등급 B를 구성할 공개 데이터의 범위와 신뢰도는 충분한가? | 미결 | 구현 담당 | - |
| 5 | Phase 1 결과에 따라 어느 위험 레짐 정책을 승인하는가? | 미결 | 사용자 | - |
| 6 | 실계좌 소액 자본 상한은 얼마인가? | 미결 | 사용자 | - |
| 7 | 텔레그램 장애를 감지할 외부 dead-man 채널은 무엇인가? | 미결 | 사용자 | - |
| 8 | Alpha Vantage의 현재 호출 한도와 데이터 사용 조건이 운영 요구사항을 충족하는가? | 미결 | 구현 담당 | - |

---

## 9. 변경 이력 (Changelog)

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| v1.0 | 2026-07-21 | 원본 설계를 구현 가능한 계약과 검증 기준으로 재구성 | Codex |
| v1.1 | 2026-07-21 | 기준 스펙 승격과 레짐·팩터·PIT·정합성 정책 통합 | Codex |

---

## 10. 참고 자료 (References)

- 문서 운영 규칙: [README.md](../README.md)
- 원본 설계: [2026-07-21-trade-flow-design.md](2026-07-21-trade-flow-design.md)
- KIS Developers API 문서: 구현 착수 전 공식 문서 링크와 확인일을 기록한다.
- Alpha Vantage NEWS_SENTIMENT 문서: 구현 착수 전 공식 문서 링크와 확인일을 기록한다.
- 미국시장 캘린더: 사용 라이브러리와 버전을 구현 manifest에 기록한다.
